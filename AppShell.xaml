<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DvijCShapr.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DvijCShapr"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Мероприятия"
        ContentTemplate="{DataTemplate local:StockFavPage}"
        Route="StockFavPage" />

    <ShellContent
        Title="Мероприятия"
        ContentTemplate="{DataTemplate local:StocksAllPage}"
        Route="StocksAllPage" />-->

    <TabBar>

        <Tab Title="Мероприятия">

            <ShellContent
                Title="Мероприятия"
                ContentTemplate="{DataTemplate local:MeetingsPage}"
                Route="MeetingsPage" />
            <ShellContent
                Title="Акции"
                ContentTemplate="{DataTemplate local:StocksPage}"
                Route="StocksPage" />
            <ShellContent
                Title="Профиль"
                ContentTemplate="{DataTemplate local:ProfilePage}"
                Route="ProfilePage" />
            </Tab>
        <ShellContent
            Title="Заведения"
            ContentTemplate="{DataTemplate local:PlacesPage}"
            Route="PlacesPage" />
       
    </TabBar>

</Shell>
