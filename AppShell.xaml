<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DvijCShapr.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DvijCShapr"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>

       
        <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

        <ShellContent
        Title="Профиль"
        ContentTemplate="{DataTemplate local:ProfilePage}"
        Route="ProfilePage" />

        <ShellContent
        Title="Мероприятия"
        ContentTemplate="{DataTemplate local:MeetingsPage}"
        Route="MeetingsPage" />

        <ShellContent
        Title="Заведения"
        ContentTemplate="{DataTemplate local:PlacesPage}"
        Route="PlacesPage" />

        <ShellContent
        Title="Акции"
        ContentTemplate="{DataTemplate local:StocksPage}"
        Route="StocksPage" />
        
    </TabBar>
    
    

</Shell>
